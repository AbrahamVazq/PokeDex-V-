//  Namespace.swift
//  PokeDex(V)
//  Created by Moises Abraham Vazquez Perez on 13/02/23.

import Foundation

// MARK: - Namespace

///  Enum containing several extensions in charge of returning the corresponding colors, constants, images and others elements to build the app theme.
enum Namespace {
    
    /// The Constant enum allows to define several extensions working as a pure namespace. Extensions might include common sizes, strings and more used through the app.
    ///
    ///  - Note: The initial declaration of the enum is empty for coding purposes.
    enum Constant { }
}
